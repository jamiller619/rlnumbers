/**
 * This module is responsible for watching directories for new
 * replay files, and saving this state to a database. This
 * database will be read from the Electron app, and will
 * import any new replay files when the app opens next.
 *
 */
